# Knock-Knock-Delivery

# Installation
- composer install
- npm install

# Create .env file (can be based on .env.example)
- php artisan key:generate

# Start angular 
- ng serve

# Build
- ng build

# Run Laravel
- php artisan serve
