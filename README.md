# Knock-Knock-Delivery

# Installation
- composer install
- npm install

# Create .env file (can be based on .env.example)
- php artisan key:generate

# Build
- ng build

# Run Laravel
- php artisan serve
